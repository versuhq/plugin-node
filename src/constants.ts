/** Standard filename for Node.js package descriptor. */
export const NODE_PACKAGE_FILE = "package.json";

/** Standard filename for pnpm workspace configuration. */
export const PNPM_WORKSPACE_FILE = "pnpm-workspace.yaml";

/** Unique identifier for the Node.js adapter ('node'). */
export const NODE_ID = "node";
